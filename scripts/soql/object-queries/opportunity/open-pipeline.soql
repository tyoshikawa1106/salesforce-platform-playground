// Run: sf data query --file scripts/soql/object-queries/opportunity/open-pipeline.soql --target-org <alias>

SELECT Id, Name, Account.Name, StageName, Amount, CloseDate, Owner.Name
FROM Opportunity
WHERE IsClosed = FALSE
ORDER BY CloseDate ASC
LIMIT 50
