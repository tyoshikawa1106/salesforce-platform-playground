// Run: sf data query --file scripts/soql/object-queries/account/duplicate-candidates.soql --target-org <alias>

SELECT Name, COUNT(Id)
FROM Account
GROUP BY Name
HAVING COUNT(Id) > 1
ORDER BY COUNT(Id) DESC
LIMIT 50
