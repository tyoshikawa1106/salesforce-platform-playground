
SELECT Id, CaseNumber, Subject, Account.Name, Status, Priority, CreatedDate, LastModifiedDate, Owner.Name
FROM Case
WHERE IsClosed = FALSE AND (Priority = 'High' OR CreatedDate < LAST_N_DAYS:7)
ORDER BY CreatedDate ASC
LIMIT 50
