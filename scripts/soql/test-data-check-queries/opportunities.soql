// Run: sf data query --file scripts/soql/test-data-check-queries/opportunities.soql --target-org <alias>

SELECT Id, Name, Account.Name, StageName, Amount, CloseDate, CreatedDate
FROM Opportunity
ORDER BY CreatedDate DESC
LIMIT 200
