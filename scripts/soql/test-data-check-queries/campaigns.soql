// Run: sf data query --file scripts/soql/test-data-check-queries/campaigns.soql --target-org <alias>

SELECT Id, Name, Type, Status, IsActive, StartDate, EndDate, CreatedDate
FROM Campaign
ORDER BY StartDate DESC
LIMIT 200
