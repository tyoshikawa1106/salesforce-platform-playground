// Run: sf data query --file scripts/soql/test-data-check-queries/contracts.soql --target-org <alias>

SELECT Id, ContractNumber, Account.Name, Status, StartDate, ContractTerm, CreatedDate
FROM Contract
ORDER BY CreatedDate DESC
LIMIT 200
