// Run: sf data query --file scripts/soql/test-data-check-queries/cases.soql --target-org <alias>

SELECT Id, CaseNumber, Subject, Account.Name, Status, Priority, CreatedDate
FROM Case
ORDER BY CreatedDate DESC
LIMIT 200
