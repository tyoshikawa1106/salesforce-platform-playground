// Run: sf data query --file scripts/soql/test-data-check-queries/orders.soql --target-org <alias>

SELECT Id, OrderNumber, Account.Name, Contract.ContractNumber, Status, Type, EffectiveDate, CreatedDate
FROM Order
ORDER BY CreatedDate DESC
LIMIT 200
