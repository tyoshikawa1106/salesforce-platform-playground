// Run: sf data query --file scripts/soql/test-data-check-queries/service-contracts.soql --target-org <alias>

SELECT Id, Name, Account.Name, Contact.LastName, Status, StartDate, EndDate, CreatedDate
FROM ServiceContract
ORDER BY CreatedDate DESC
LIMIT 200
