// Run: sf data query --file scripts/soql/case/open-cases.soql --target-org <alias>

SELECT Id, CaseNumber, Subject, Account.Name, Status, Priority, Owner.Name, CreatedDate
FROM Case
WHERE IsClosed = FALSE
ORDER BY CreatedDate ASC
LIMIT 50
