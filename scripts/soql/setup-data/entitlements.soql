// Run: sf data query --file scripts/soql/setup-data/entitlements.soql --target-org <alias>

SELECT Id, Name, Account.Name, Contact.LastName, Asset.Name, ServiceContract.Name, StartDate, EndDate, CreatedDate
FROM Entitlement
ORDER BY CreatedDate DESC
LIMIT 200
