// Run: sf data query --file scripts/soql/setup-data/contacts.soql --target-org <alias>

SELECT Id, FirstName, LastName, Account.Name, Email, Title, CreatedDate
FROM Contact
ORDER BY CreatedDate DESC
LIMIT 200
