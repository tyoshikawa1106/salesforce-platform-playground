// Run: sf data query --file scripts/soql/setup-data/tasks.soql --target-org <alias>

SELECT Id, Subject, Who.Name, What.Name, Status, Priority, ActivityDate, CreatedDate
FROM Task
ORDER BY CreatedDate DESC
LIMIT 200
