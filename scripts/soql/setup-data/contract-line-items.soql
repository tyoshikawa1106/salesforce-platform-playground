// Run: sf data query --file scripts/soql/setup-data/contract-line-items.soql --target-org <alias>

SELECT Id, ServiceContract.Name, Asset.Name, PricebookEntry.Product2.Name, Quantity, UnitPrice, StartDate, EndDate, CreatedDate
FROM ContractLineItem
ORDER BY CreatedDate DESC
LIMIT 200
