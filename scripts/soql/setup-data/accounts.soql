// Run: sf data query --file scripts/soql/setup-data/accounts.soql --target-org <alias>

SELECT Id, Name, AccountNumber, Industry, Type, CreatedDate
FROM Account
WHERE Name LIKE '[TEST]%'
ORDER BY CreatedDate DESC
LIMIT 20
