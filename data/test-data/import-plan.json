{
    "description": "Synthetic test data import plan. Run dry-run first, then execute with --target-org.",
    "imports": [
        {
            "label": "accounts",
            "operation": "insert",
            "sobject": "Account",
            "file": "data/test-data/accounts.csv",
            "wait": 30
        }
    ]
}
